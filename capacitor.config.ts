import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projeto_casa_acolhimento_grupo10',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
